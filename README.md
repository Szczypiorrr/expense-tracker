# expense-tracker
A simple expense tracker application built with Python and SQLAlchemy. The project allows users to manage expenses, organize them into categories, and generate spending reports using a SQLite database.
